<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:OneHundredCommonThings"
             x:Class="OneHundredCommonThings.Screen.EnglishScreen">
  <StackLayout>
    <ListView x:Name="RssView" ItemsSource="{Binding}" IsPullToRefreshEnabled="True"
              Refreshing="RssView_Refreshing"
              IsRefreshing="{Binding IsBusy, Mode=OneWay}"
              ItemTapped="RssView_ItemTapped" ListView.RowHeight="100">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
              <StackLayout Padding="5">
                <Label Text="{Binding SentenceId}" />
                <Label Text="{Binding Sentence}" />
                <Label Text="{Binding Meaning}"/>
              </StackLayout>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </StackLayout>
</ContentPage>